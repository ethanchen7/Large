#comment-modal.hideCommentModal
    #comment-modal-header
        #comment-modal-title= `Responses (${comments.length})` 
        Img#comment-close-out(src="/images/close.png")
    #new-comment-container
        textarea(type="text" placeholder="what are your thoughts?" spellcheck=true)#new-comment-box
        #new-comment-container-footer
            #new-comment-cancel Cancel
            #new-comment-submit Respond
    #sort-by Top comments
    #comment-list 
        - if (comments.length === 0)
            #wow-empty Wow, such empty :/
        each comment in comments
            .comment-container 
                .comment-container-header 
                    .comment-user-img 
                        img(src="/images/logo.png")
                    .comment-user-info 
                        a.comment-author(href=`/users/${comment.userId}`)= `${comment.firstName} ${comment.lastName}`
                        .comment-daysAgo= `${comment.daysAgo}`
                .comment-content= comment.text
            //- .comment-container-footer 
            //-     .comment-clap
            //-         img(src="/images/clap.png", alt="")
            //-         .comment-clap-count= `1`
            //-     a(href="").comment-reply Reply

